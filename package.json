{
  "name": "ipa-manager-api",
  "version": "1.0.0",
  "private": true,
  "description": "Simple API to upload IPA files and return OTA install links.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "bplist-parser": "^0.3.2",
    "dotenv": "^16.5.0",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "plist": "^3.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.1.1"
  }
}
